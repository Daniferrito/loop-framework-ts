{
  "name": "loop-framework",
  "private": true,
  "workspaces": [
    "logic"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "concurrently": "^6.0.0"
  }
}